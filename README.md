# Useful Scripts

## Installation

To install the `golang` script to `/usr/local/bin/golang` on Linux or macOS, run the following commands:

```bash
chmod +x golang
sudo cp golang /usr/local/bin/golang
```
